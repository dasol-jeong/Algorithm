// 주어진 문자열 중 시작문자와 끝나는 문자가 동일한 가장 긴 문자열 출력
// DP알고리즘 이용
// 다시풀기

function solution(s) {
  let left = 0,
    right = 0;

  for (let i = 0; i < s.length; i++) {
    for (let j of [i, i + 1]) {
      for(){}
    }
  }

  return answer;
}
console.log(solution("babad"));
