// 강사님....전 더 배우고 연습하고 오겠습니다...나중을 기약하며...감사합니다
function solution(nums){
  let answer=0;
  let n=nums.length;
  let sum=0,right=1;
  let score=n;
  
  let nH=new Map();


  for(let i=0;i<n;i++){
    for(let j=2;j<n;j++){
      for(let x of nums[i][]){
        nH.set(x,1);
      }
    }
    
  }
  if(val)  
    
  for(let i=n-1;i>=0;i--){
    
  }
  for(let i=0;i<n-1;i++){
    for(let j=1;j<n;j++){
      if(nums[i][j]===nums[])
    }
  }
  return answer;
}
3+3+4+2+1=13
// 일단 배열뒤에서부터 뒤가 큰 점수 두더지니까, 
// 배열로부터 index[2]부터 새로운 배열로 만들어?
// 배열로부터 두더지 나타나는 수 가져와서 그만큼 돌려서 시간넣고 1씩 넣어주기 중복값 없으니까
// 두더지는 nums.length로 알고
// 1~4두더지들의 1초에 올라오는 것 중 가장 점수 큰것을 sum에 넣고
// 1~4두더지들의 2초에 올라오는 것 중 가장 점수 큰것을 sum에 넣고
// 1~4두더지들의 3초에 올라오는 것 중 가장 점수 큰것을 sum에 넣고
// 1~4두더지들의 4초에 올라오는 것 중 가장 점수 큰것을 sum에 넣으면 
for(let x of nums[i][]){
      nH.set(x,1);
    }